# Dev: пусто — Vite проксирует /api на backend :8000.
# Docker/prod: пусто — сборка подставит /mes/api (запросы /mes/api/api/v1).
# Контейнер web проксирует и /mes/api/api/, и /mes/api/.

VITE_API_BASE=
